var array = [1, 2, 3, 4, 5];
for ( var i = array.length-1; i >=0; i--){ 
	document.write(array[i] + ', '); //expected 5, 4, 3, 2, 1
}

-------------------------------func---------------------------------
function arrayReverse(array) {
	let newArr = [];
	
	for (let i = array.length - 1; i >= 0; i--) {
		newArr.push(array[i]);
	}
        
	console.log(newArr);
}

arrayReverse([1, 2, 3, 4, 5]); //expected [5, 4, 3, 2, 1]
