gulp.task('watch', function() {
	gulp.watch('app/sass/**/*.sass', gulp.series('sass')); /* had a problem in execution "watch task has to be a function", then i added "gulp.series" 
  and solve that prob. Be attentive. */
});
